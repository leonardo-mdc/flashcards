name: Deploy to InfinityFree

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      # ── Main site ──────────────────────────────────────────────
      - name: Backup config.php (main)
        run: |
          curl -sS -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            "ftp://ftpupload.net/flashcards.my-board.org/htdocs/config.php" \
            -o /tmp/config-main.php.backup 2>/dev/null || \
            echo "No existing config.php (main)"

      - name: Sync files to main site
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ftpupload.net
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /flashcards.my-board.org/htdocs/
          exclude: |
            config.php
            .git*
          dangerous-clean-slate: false

      - name: Restore config.php (main)
        run: |
          if [ -f /tmp/config-main.php.backup ] && [ -s /tmp/config-main.php.backup ]; then
            curl -sS -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
              -T /tmp/config-main.php.backup \
              "ftp://ftpupload.net/flashcards.my-board.org/htdocs/config.php"
            echo "config.php restored (main)"
          else
            echo "Upload config.php manually via InfinityFree file manager for main site"
          fi

      # ── Mirror site ─────────────────────────────────────────────
      - name: Backup config.php (mirror)
        run: |
          curl -sS -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            "ftp://ftpupload.net/flashcards.free.nf/htdocs/config.php" \
            -o /tmp/config-mirror.php.backup 2>/dev/null || \
            echo "No existing config.php (mirror)"

      - name: Sync files to mirror site
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ftpupload.net
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /flashcards.free.nf/htdocs/
          exclude: |
            config.php
            .git*
          dangerous-clean-slate: false

      - name: Restore config.php (mirror)
        run: |
          if [ -f /tmp/config-mirror.php.backup ] && [ -s /tmp/config-mirror.php.backup ]; then
            curl -sS -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
              -T /tmp/config-mirror.php.backup \
              "ftp://ftpupload.net/flashcards.free.nf/htdocs/config.php"
            echo "config.php restored (mirror)"
          else
            echo "Upload config.php manually via InfinityFree file manager for mirror site"
          fi
